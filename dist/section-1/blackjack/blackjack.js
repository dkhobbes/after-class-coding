

function countCards(cards) {
  var count = 0;
  for (var i = 0; i < CardAmount.length; i++) {
    var card = cards.CardAmount[i];
    console.log(card.number);
    if (card.number       ) {

    }
  }
  return count;
}
